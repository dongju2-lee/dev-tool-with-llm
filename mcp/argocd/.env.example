# ArgoCD MCP 서버 환경 변수 설정
# 이 파일을 .env로 복사하고 값을 환경에 맞게 수정하세요

# 서버 설정
MCP_HOST=0.0.0.0
MCP_PORT=8000

# ArgoCD 연결 설정 
# 쿠버네티스 내부: https://argocd-server.argocd.svc.cluster.local
# 외부 접속: https://argocd.example.com
ARGOCD_SERVER=https://argocd-server.argocd.svc.cluster.local

# ArgoCD API 토큰
# 토큰 생성 방법: argocd account generate-token --account admin
ARGOCD_TOKEN=your-argocd-token-here

# 로깅 설정
LOG_LEVEL=INFO

# TLS 인증서 검증 (개발 환경에서는 false로 설정 가능)
# 프로덕션 환경에서는 true로 설정 권장
VERIFY_SSL=false

# 기본 프로젝트 및 클러스터 설정
DEFAULT_PROJECT=default
DEFAULT_CLUSTER=https://kubernetes.default.svc
DEFAULT_NAMESPACE=default 